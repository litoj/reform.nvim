```lua
(upvalue) top_identity: fun(opts: string|fzf-lua.test|manipulator.ts.PresetOpts<manipulator.ts.NodeOpts>, node?: TSNode, ltree: vim.treesitter.LanguageTree, return_par...(too long)...uageTree)?
```

---

Get the furthest ancestor with the same range as current node (and its parent if {parent})

---

```lua
function top_identity(opts: string|fzf-lua.test|Opts<ts.NodeOpts,<`B`>,<C:{x:integer}>>, node: TSNode?, ltree: vim.treesitter.LanguageTree, return_parent: boolean?)
  -> TSNode?
  2. (vim.treesitter.LanguageTree)?
```
